services:
  web-app:
    build: .
    ports:
      - "8501:8501"
    environment:
      - PYTHONUNBUFFERED=1
      - OCR_BACKEND=auto
      - OCR_DEVICE=auto
    command: streamlit run app.py --server.port=8501 --server.address=0.0.0.0 --server.fileWatcherType=none
